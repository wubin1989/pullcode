/**
* Generated by pullcode <%- version %>.
* Don't edit!
*
* @module types
*/

<%_ if(types && types.length){ -%>
  <%_ types.forEach(function(type){ -%>
    <%_ if(type.doc){ -%>
/**
* <%- type.doc %>
*/
    <%_ } -%>
export interface <%- type.name %> {
    <%_ if(type.properties && type.properties.length){ -%>
      <%_ type.properties.forEach(function(property){ -%>
        <%_ if(property.doc){ -%>
  /**
  * <%- property.doc %>
  */
        <%_ } -%>
  <%- property.name %><% if(!property.required){ %>?<% } %>: <%- property.type %>;
      <%_ }) -%>
    <%_ } -%>
}

    <%_ if(type.enumTypes && type.enumTypes.length){ -%>
      <%_ type.enumTypes.forEach(function(enumType){ -%>
export enum <%- enumType.name %> {
        <%_ if(enumType.enums && enumType.enums.length){ -%>
          <%_ enumType.enums.forEach(function(item){ -%>
  <%- item.name %> = "<%- item.value %>",
          <%_ }) -%>
        <%_ } -%>
}
      <%_ }) -%>
    <%_ } -%>
  <%_ }) -%>
<%_ } -%>